# Deployments

No public deployment has been made from this repository yet.

After a Fuji deployment, add `fuji.json` with the chain ID, deployer, block number,
transaction hashes, contract addresses, source commit, and UTC timestamp. Never add a
private key or authenticated RPC URL.

